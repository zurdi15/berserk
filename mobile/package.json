{
  "name": "berserk-mobile",
  "private": true,
  "version": "0.36.1",
  "description": "Shell Android (Capacitor) de berserk — WebView contra el servidor",
  "scripts": {
    "sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
